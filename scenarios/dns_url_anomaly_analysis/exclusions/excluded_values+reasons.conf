# One value + reason rule per line
# Format: <value> || <reason>
# Reason must match one item from the reasons list (case-insensitive)
# Examples:
# xn--example-domain-123.com || punycode
# suspicious-domain.tld || high-entropy

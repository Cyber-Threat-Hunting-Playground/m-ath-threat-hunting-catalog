# One value + reason rule per line
# Format: <value> || <reason>
# Reason must match one item from the reasons list used by the scenario notebook (case-insensitive)
# Example:
# C:\Program Files\App\known-good.dll || known-vendor-path

# One value per line to exclude completely (e.g. full DLL path, SHA-256, or normalized path fragment)
# Lines starting with # are comments
# Example:
# C:\Program Files\LegitimateVendor\benign-helper.dll

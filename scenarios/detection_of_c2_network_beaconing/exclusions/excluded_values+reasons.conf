# One value + reason rule per line
# Format: <value> || <reason>
# Reason must match one item from the reasons list passed to the exclusion check (case-insensitive)
# Examples:
# 10.0.0.0 || internal-net
# noisy-cdn.example || high-volume

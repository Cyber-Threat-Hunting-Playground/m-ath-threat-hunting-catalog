# Destination IPs or hostnames reviewed and confirmed as genuine findings — NOT false positives
# One value per line (lowercased when matched)
# Lines starting with # are comments
#
# Destinations listed here will be excluded from c2_network_beaconing_false_positive_candidates.csv
# so they are not re-proposed for exclusion on subsequent runs.
#
# Workflow:
#   1. Run the notebook → review output/c2_network_beaconing_false_positive_candidates.csv
#   2. For destinations you want to EXCLUDE (true false positives): add them to excluded_values.conf
#   3. For destinations you want to KEEP in findings (genuine threats): add them here
#
# Example:
# suspicious-beacon.example.com
# 10.20.30.40
